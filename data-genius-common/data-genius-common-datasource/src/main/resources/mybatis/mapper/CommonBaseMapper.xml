<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.fasterTool.common.datasource.mapper.CommonBaseMapper">
    <insert id="insertMethod" parameterType="java.lang.String">
        ${sql}
    </insert>
    <update id="updateMethod" parameterType="java.lang.String">
        ${sql}
    </update>
    <delete id="delete" parameterType="java.lang.String">
        ${sql}
    </delete>
    <select id="findById" resultType="java.util.Map" parameterType="java.lang.String">
        ${sql}
    </select>
    <select id="count" resultType="int" parameterType="java.lang.String">
        ${sql}
    </select>
    <select id="query" resultType="java.util.Map" parameterType="java.lang.String">
        ${sql}
    </select>

</mapper>